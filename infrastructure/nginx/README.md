# Nginx Infrastructure

This folder is reserved for future reverse-proxy and edge-routing configuration if the project later needs a dedicated web gateway.

No nginx runtime configuration is required for the minimal local skeleton.
