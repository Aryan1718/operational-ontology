# Docker Infrastructure

This folder is reserved for future container build, runtime, and deployment support assets that go beyond the minimal local development setup.

The current skeleton relies on the root `docker-compose.yml` and service Dockerfiles only.
