import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

export default function OntologyPage() {
  return (
    <FeaturePlaceholder
      title="Ontology Studio placeholder"
      description="This area will become the read-only control-plane view over ontology metadata, object types, links, functions, actions, and permissions."
    />
  );
}
